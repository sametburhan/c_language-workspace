//ders12
#include <stdio.h>
#include <string.h>
/*
int main() {
    int i,p=0;
    char a;
    scanf("%c \n",&a);
    char abc[100];
    fgets(abc, sizeof(abc), stdin);
    for(i=0;i<strlen(abc);i++){
        if(abc[i]==a){
            printf("%c\n",abc[i]);
            printf("%d.karakterde %c bulunmaktadır",i,a);
            p++;
        }else
        puts("-");
    }printf("toplam %d tane %c karakteri vardır",p,a);
    return 0;

}----------------------------------------------------------*/


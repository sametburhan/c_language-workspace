//ders10
#include <stdio.h>
#include <stdlib.h>

/*
	int main(void) {

		int N, X;
		int i, j;

		scanf("%d %d",&N, &X);

		int arr[N];

		for(i = 0; i < N; i++)
		{
			scanf("%d",&arr[i]);
		}

		for(i = 0; i < N-1; i++)
		{
			for(j = i+1; j < N; j++)
			{
				if(arr[i] + arr[j] == X)//darklı indexlerdeki dizi değerleri toplamı
					//sonradan girmiş olduğumuz sayı değerine esitse o indexlerdeki
					//sayı değerleri ekrana yazıdırılır
					printf("%d %d\n", arr[i], arr[j]);
			}
		}
		return 0;
}---------------------------------------------------------------*/








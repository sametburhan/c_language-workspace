//ders 5
#include <stdio.h>
#include <stdlib.h>

int main() {
	/*
	char c;
		scanf("%c", &c);

		if(c >= 'a' && c <= 'z'){
			printf("%c", c+('A'-'a'));
		}
		else if(c >= 'A' && c <= 'Z'){//&& ve, || veya
			printf("%c", c+('a'-'A'));
		}
		else if(c >= '0' && c <= '9'){
			printf("%d", 2*(c-'0'));
		}
		else{
			printf("asdfasdf");
		}
	-----------------------------------------------------*/
	int N,M;
	scanf("%d %d",&N,&M);
	int k = 0,t = 0;
	while(k<N){
		while(t<M){
		printf("* ");
		t++;
		}
		printf("\n");
		k++;
	}

	return 0;
}

//ders 5
#include <stdio.h>
#include <stdlib.h>

int main() {
	/*
	char c;
		scanf("%c", &c);

		if(c >= 'a' && c <= 'z'){
			printf("%c", c+('A'-'a'));
		}
		else if(c >= 'A' && c <= 'Z'){//&& ve, || veya
			printf("%c", c+('a'-'A'));
		}
		else if(c >= '0' && c <= '9'){
			printf("%d", 2*(c-'0'));
		}
		else{
			printf("asdfasdf");
		}
	-----------------------------------------------------*/
	int a,b;
		scanf("%d %d",&a,&b);
		int t=0,k=0;
		while(t<a){
		    while(k<b){
		        printf("*");
		        k++;
		    }
		    printf("\n");
		    t++;
		}//

	return 0;
}

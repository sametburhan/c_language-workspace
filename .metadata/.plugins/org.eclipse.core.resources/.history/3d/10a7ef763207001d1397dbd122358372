//ders3
#include <stdio.h>
#include <stdlib.h>

int main() {
	/*
	int second,minute,hour;
	scanf("%d %d %d", &second, &minute, &hour);//virgüller önemli

	int hours_to_second = hour*3600;
	int minutes_to_second = minute*60;
	int total_seconds = hours_to_second+minutes_to_second+second;
	printf("toplam %d saniye", total_seconds);
	------------------------------------------------------------------------------------------*/
	char ch; //char veri tipi 1 bitlik karakterden oluşur; a, b, c, birer karakterdir
	scanf("%c",&ch);//kullanıcıdan char verisi çekebilmek içimn %c kodu kullanılır
	printf("%d",ch-'A'+1);

	return EXIT_SUCCESS;
}
